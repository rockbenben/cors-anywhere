# 仅用于本地调试。生产构建走 GitHub Actions：
# 发布 release 或手动触发 workflow 后，会自动构建多架构镜像并推送到
# Docker Hub (rockben/cors-anywhere) 和 GHCR (ghcr.io/rockbenben/cors-anywhere)。
# 详见 .github/workflows/docker.yml。

# 本地构建镜像
sudo docker build -t cors-anywhere .

# 本地运行容器
sudo docker run -d -p 49152:8080 --name cors-anywhere cors-anywhere
